//using System.Collections;
//using System.Collections.Generic;
//using UnityEngine;

//public class TriggerCanvas3 : MonoBehaviour {

	// Use this for initialization
	//void Start () {

       // WWW www = new WWW("http://www.cems.uwe.ac.uk/~le2-nunez/Songkick");
       // yield return www;
       // string json = www.text;
        //JSONObject jsonObject = JSON.Parse(str);
       // JSONObject image = jsonObject.GetObject("");
      //  string name = image.GetString("");

    //}
	
	// Update is called once per frame
	//void Update () {
		
	//}
//}
